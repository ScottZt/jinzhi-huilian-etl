## 📋 示例18 详解：官方插件 · 停牌日填充（A 股缺失数据处理）

### 🎯 目标

演示官方精选插件 `fill_suspended`。模拟 A 股停牌：故意删除第 5/15/30 个交易日的数据，用前值填充价格列、成交量填 0，并输出 `is_suspended` 停牌标记列。

---

### 🔗 节点流程图

```
┌─ 生成有停牌缺口数据 (custom_python)
  ┌─ 停牌日填充 (fill_suspended)
    ┌─ 写入DuckDB (target_write)
```

---

### 📦 各节点详解

#### 1️⃣ n1: 生成有停牌缺口数据

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n1` |
| 节点类型 | `custom_python` |
| 功能 | 自定义 Python 脚本 |
| 参数 | {"code": "def process(df):\n    import pandas as pd, numpy as np\n    np.random.seed(42)\n    dates = pd.bdate_range(end='2026-07-22', periods=60)\n    price = 10 * np.cumprod(1 + np.random.normal(0,  |

#### 2️⃣ n2: 停牌日填充

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n2` |
| 节点类型 | `fill_suspended` |
| 功能 | 停牌日填充（官方插件） |
| 参数 | {"code_column": "code", "date_column": "dt", "price_columns": "open,high,low,close", "volume_column": "vol"} |

#### 3️⃣ n3: 写入DuckDB

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n3` |
| 节点类型 | `target_write` |
| 功能 | 将处理结果写入目标库 |
| 参数 | {"target_type": "duckdb", "target_config": "{\"db_path\": \"D:/data/official_demo.db\"}", "target_table": "official_fill_suspended_demo", "batch_size": 5000, "on_duplicate": "replace", "columns": ""} |

---

### 💡 使用场景

本示例适用于需要**演示官方精选插件 `fill_suspended`。模拟 A 股停牌：故意删除第 5/15/30 个**的场景。

### ⚠️ 注意事项

1. 确保已配置对应的数据源连接
2. 根据实际数据调整参数配置
3. 大数据量时建议分批处理
