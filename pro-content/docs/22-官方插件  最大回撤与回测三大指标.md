## 📋 示例22 详解：官方插件 · 最大回撤与回测三大指标

### 🎯 目标

演示官方精选插件 `max_drawdown`。输入净值序列，一次算完最大回撤、年化收益、年化波动、夏普比率、胜率、回撤起止日期等 9 个核心回测指标。

---

### 🔗 节点流程图

```
┌─ 生成净值序列 (custom_python)
  ┌─ 计算回测三大指标 (max_drawdown)
    ┌─ 写入DuckDB (target_write)
```

---

### 📦 各节点详解

#### 1️⃣ n1: 生成净值序列

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n1` |
| 节点类型 | `custom_python` |
| 功能 | 自定义 Python 脚本 |
| 参数 | {"code": "def process(df):\n    import pandas as pd, numpy as np\n    np.random.seed(42)\n    n = 252  # 一年交易日\n    dates = pd.bdate_range(end='2026-07-22', periods=n)\n    # 模拟一个带回撤的净值曲线\n    ret = n |

#### 2️⃣ n2: 计算回测三大指标

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n2` |
| 节点类型 | `max_drawdown` |
| 功能 | 最大回撤计算（官方插件） |
| 参数 | {"net_value_column": "close", "risk_free_rate": 0.02, "trading_days_per_year": 252, "output_prefix": "backtest"} |

#### 3️⃣ n3: 写入DuckDB

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n3` |
| 节点类型 | `target_write` |
| 功能 | 将处理结果写入目标库 |
| 参数 | {"target_type": "duckdb", "target_config": "{\"db_path\": \"D:/data/official_demo.db\"}", "target_table": "official_backtest_demo", "batch_size": 5000, "on_duplicate": "replace", "columns": ""} |

---

### 💡 使用场景

本示例适用于需要**演示官方精选插件 `max_drawdown`。输入净值序列，一次算完最大回撤、年化收益、年化波动、**的场景。

### ⚠️ 注意事项

1. 确保已配置对应的数据源连接
2. 根据实际数据调整参数配置
3. 大数据量时建议分批处理
