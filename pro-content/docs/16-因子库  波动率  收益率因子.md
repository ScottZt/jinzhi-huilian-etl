## 📋 示例16 详解：因子库 — 波动率 + 收益率因子

### 🎯 目标

计算 1 日收益率和 20 日年化波动率因子。这类统计因子在量化策略中广泛使用。

---

### 🔗 节点流程图

```
┌─ 拉取日 K 线 (source_fetch)
  ┌─ 计算 1 日收益率 (factor_compute)
    ┌─ 写入收益率因子 (factor_write)
  ┌─ 计算 20 日波动率 (factor_compute)
    ┌─ 写入波动率因子 (factor_write)
```

---

### 📦 各节点详解

#### 1️⃣ n1: 拉取日 K 线

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n1` |
| 节点类型 | `source_fetch` |
| 功能 | 从数据源拉取原始数据 |
| 参数 | {"source_type": "tdx", "source_config": "{\"data_dir\": \"D:/new_tdx64/vipdoc\"}", "codes": "000001", "interval": "D", "time_mode": "lookback", "lookback_days": 100, "parallel": false, "session_only": |

#### 2️⃣ n2: 计算 1 日收益率

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n2` |
| 节点类型 | `factor_compute` |
| 功能 | 因子计算 |
| 参数 | {"factor_id": "ret_1d", "compute_type": "return", "params_json": "{\"window\": 1}", "source_column": "close"} |

#### 3️⃣ n3: 写入收益率因子

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n3` |
| 节点类型 | `factor_write` |
| 功能 | 因子写入 |
| 参数 | {"factor_id": "ret_1d", "db_path": "D:/data/factor_data.duckdb", "write_mode": "upsert", "register_meta": true} |

#### 4️⃣ n4: 计算 20 日波动率

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n4` |
| 节点类型 | `factor_compute` |
| 功能 | 因子计算 |
| 参数 | {"factor_id": "volatility_20", "compute_type": "volatility", "params_json": "{\"window\": 20}", "source_column": "close"} |

#### 5️⃣ n5: 写入波动率因子

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n5` |
| 节点类型 | `factor_write` |
| 功能 | 因子写入 |
| 参数 | {"factor_id": "volatility_20", "db_path": "D:/data/factor_data.duckdb", "write_mode": "upsert", "register_meta": true} |

---

### 💡 使用场景

本示例适用于需要**计算 1 日收益率和 20 日年化波动率因子。这类统计因子在量化策略中广泛使用。**的场景。

### ⚠️ 注意事项

1. 确保已配置对应的数据源连接
2. 根据实际数据调整参数配置
3. 大数据量时建议分批处理
