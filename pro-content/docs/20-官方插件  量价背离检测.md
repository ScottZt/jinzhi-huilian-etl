## 📋 示例20 详解：官方插件 · 量价背离检测

### 🎯 目标

演示官方精选插件 `volume_price_divergence`。识别四种经典信号：顶背离（价涨量缩=1）/ 恐慌放量（价跌量增=-1）/ 底背离（价跌量缩=2）/ 无背离（0）。

---

### 🔗 节点流程图

```
┌─ 生成量价数据 (custom_python)
  ┌─ 量价背离检测 (volume_price_divergence)
    ┌─ 写入DuckDB (target_write)
```

---

### 📦 各节点详解

#### 1️⃣ n1: 生成量价数据

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n1` |
| 节点类型 | `custom_python` |
| 功能 | 自定义 Python 脚本 |
| 参数 | {"code": "def process(df):\n    import pandas as pd, numpy as np\n    np.random.seed(42)\n    n = 60\n    dates = pd.bdate_range(end='2026-07-22', periods=n)\n    price = 10 * np.cumprod(1 + np.random |

#### 2️⃣ n2: 量价背离检测

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n2` |
| 节点类型 | `volume_price_divergence` |
| 功能 | 量价背离检测（官方插件） |
| 参数 | {"price_column": "close", "volume_column": "vol", "window": 5, "threshold": 0.3} |

#### 3️⃣ n3: 写入DuckDB

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n3` |
| 节点类型 | `target_write` |
| 功能 | 将处理结果写入目标库 |
| 参数 | {"target_type": "duckdb", "target_config": "{\"db_path\": \"D:/data/official_demo.db\"}", "target_table": "official_vp_demo", "batch_size": 5000, "on_duplicate": "replace", "columns": ""} |

---

### 💡 使用场景

本示例适用于需要**演示官方精选插件 `volume_price_divergence`。识别四种经典信号：顶背离（价涨**的场景。

### ⚠️ 注意事项

1. 确保已配置对应的数据源连接
2. 根据实际数据调整参数配置
3. 大数据量时建议分批处理
