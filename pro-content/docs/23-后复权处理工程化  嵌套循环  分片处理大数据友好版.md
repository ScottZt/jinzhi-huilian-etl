## 📋 示例23 详解：后复权处理工程化 — 嵌套循环 + 分片处理（大数据友好版）

### 🎯 目标

采用嵌套循环架构：外层遍历 7 张表，内层遍历每个股票代码。每次只处理一只股票的数据，内存占用极低。从 baostock 拉取 fore+back 因子，输出未复权/前复权/后复权 3 张表。共 21 张目标表。

---

### 🔗 节点流程图

```
┌─ 设置表名列表 (set_variable)
  ┌─ 遍历表（外层循环） (for_each)
    ┌─ 获取股票列表 (custom_python)
      ┌─ 遍历股票（内层循环） (for_each)
        ┌─ 处理单只股票复权 (custom_python)
          ┌─ 股票间隔等待 (wait)
            ┌─ 表间隔等待 (wait)
```

---

### 📦 各节点详解

#### 1️⃣ n1: 设置表名列表

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n1` |
| 节点类型 | `set_variable` |
| 功能 | 设置变量 |
| 参数 | {"var_name": "tables", "var_value": "[\"dat_day\",\"dat_60mins\",\"dat_30mins\",\"dat_15mins\",\"dat_10mins\",\"dat_5mins\",\"dat_1mins\"]", "value_type": "json"} |

#### 2️⃣ n2: 遍历表（外层循环）

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n2` |
| 节点类型 | `for_each` |
| 功能 | 循环遍历 |
| 参数 | {"items": "{{tables}}", "item_var": "current_table", "index_var": "table_index", "max_iterations": 10} |

#### 3️⃣ n3: 获取股票列表

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n3` |
| 节点类型 | `custom_python` |
| 功能 | 自定义 Python 脚本 |
| 参数 | {"code": "import duckdb\nimport pandas as pd\n\ndef process(df, context=None):\n    \"\"\"获取当前表的所有股票代码，存入 context['codes']\"\"\"\n    SOURCE_DB = 'C:/duckdb/quantifydata.duckdb'\n    current_table = c |

#### 4️⃣ n4: 遍历股票（内层循环）

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n4` |
| 节点类型 | `for_each` |
| 功能 | 循环遍历 |
| 参数 | {"items": "{{codes}}", "item_var": "current_code", "index_var": "code_index", "max_iterations": 10000} |

#### 5️⃣ n5: 处理单只股票复权

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n5` |
| 节点类型 | `custom_python` |
| 功能 | 自定义 Python 脚本 |
| 参数 | {"code": "import duckdb\nimport pandas as pd\nimport baostock as bs\n\ndef normalize_code(c):\n    c = str(c).strip().upper()\n    if '.' in c:\n        return c\n    if len(c) == 6 and c.isdigit():\n |

#### 6️⃣ n6: 股票间隔等待

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n6` |
| 节点类型 | `wait` |
| 功能 | 等待（控制频率） |
| 参数 | {"seconds": 0.1, "mode": "delay"} |

#### 7️⃣ n7: 表间隔等待

| 配置项 | 说明 |
|-------|------|
| 节点 ID | `n7` |
| 节点类型 | `wait` |
| 功能 | 等待（控制频率） |
| 参数 | {"seconds": 2, "mode": "delay"} |

---

### 💡 使用场景

本示例适用于需要**采用嵌套循环架构：外层遍历 7 张表，内层遍历每个股票代码。每次只处理一只股票的数据，内存占用极低。**的场景。

### ⚠️ 注意事项

1. 确保已配置对应的数据源连接
2. 根据实际数据调整参数配置
3. 大数据量时建议分批处理
