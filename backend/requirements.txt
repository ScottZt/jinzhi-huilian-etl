fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
pandas==2.2.3
numpy==2.1.3
sqlalchemy==2.0.36
pymysql==1.1.1
psycopg2-binary==2.9.10
duckdb==1.1.3
clickhouse-driver==0.2.9
openpyxl==3.1.5
python-multipart==0.0.12
apscheduler==3.10.4
watchdog==6.0.0
chardet==5.2.0
pyarrow==18.1.0
pystray>=0.19.4
Pillow>=10.0.0
psutil>=5.9.0
pyinstaller>=6.0.0
# Used by app.core.credential_manager (Fernet encryption/decryption)
cryptography>=43.0.0
# Used by app.api.llm and app.core.ai_script_generator for HTTP model calls.
requests>=2.32.0
# Used by app.api.credentials for credential connectivity tests.
# Keep this range compatible with mootdx 0.11.x, which requires httpx < 0.26.
httpx>=0.25.0,<0.26.0
# Used by app.adapters.source_adapters.akshare_adapter for local direct data fetching.
akshare>=1.16.98
# Used by app.adapters.source_adapters.tushare_adapter for Tushare SDK mode.
tushare>=1.2.10
# Used by app.adapters.source_adapters.mootdx_adapter for private minute K-line fetching.
mootdx>=0.11.7
# Used by app.adapters.source_adapters.tqsdk_adapter for full-market data (futures/stocks/funds/indices).
tqsdk>=3.7.0
# Used by app.adapters.source_adapters.yfinance_adapter for Yahoo Finance data.
yfinance>=0.2.40
# Used by app.adapters.source_adapters.binance_adapter for crypto market data.
python-binance>=1.0.19
# Used by example workflow 12 for automatic stock adjustment factor calculation.
baostock>=0.8.8
